const form=document.querySelector('#search-form'),input=document.querySelector('#search'),result=document.querySelector('#result'),status=document.querySelector('#status'),output=document.querySelector('#output');
function destination(v){try{const s=/^[a-z][a-z\d+.-]*:\/\//i.test(v)?v:`https://${v}`;const u=new URL(s);return['http:','https:'].includes(u.protocol)&&u.hostname?u.href:null}catch{return null}}
form.addEventListener('submit',async e=>{e.preventDefault();const v=input.value.trim();if(!v)return;const url=destination(v);const target=url||`https://www.google.com/search?q=${encodeURIComponent(v)}`;result.hidden=false;status.textContent='Loading…';output.textContent='';try{const r=await fetch(`/proxy?url=${encodeURIComponent(target)}`);status.textContent=`${r.status} ${r.statusText}`;output.textContent=await r.text()||'(empty response)'}catch(err){status.textContent='Request failed';output.textContent=err.message}});